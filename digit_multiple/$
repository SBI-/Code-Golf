#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

int digit_multiple(int n);

int main()
{
    int test[] = { 128, 12, 120, 122, 13, 32, 22, 42, 212, 213, 162, 204 };
    int length = sizeof(test) / sizeof(test[0]);

    for (int i = 0; i < length; ++i) {
        //printf("%5d -> %d\n", test[i], digit_multiple(test[i]));
        printf("%5d -> %d\n", test[i], digit_multiple(test[i]));
    }

    return EXIT_SUCCESS;
}

int digit_multiple(int n)
{
    int i;
    for(i = n; i > 0 && i % 10 && !(n % (i % 10)); i /= 10);
    return !i;
}

//bool digit_multiple(const int n)
//{
//    int i = n;
//    while (i > 0) {
//        int digit = (i % 10);
//        if (digit == 0 || n % digit != 0) {
//            return false;
//        }
//        i /= 10;
//    }
//
//    return true;
//}
